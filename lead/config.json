{
  "version": "0.0.1",
  "api_port": 5000,
  "api_host": "localhost",
  "rpc_port": 5001,
  "rpc_host": "localhost",
  "strategies": {
    "master_to_slave_replica": {
      "nb_replicas": 3,
      "losses": 3,
      "desc": "Master -> slaves"
    },
    "slave_to_slave_replica": {
      "nb_replicas": 3,
      "losses": 2,
      "desc": "Slave -> Slave"
    },
    "master_to_slave_coded": {
      "losses": 1,
      "desc": "Master -> slaves"
    },
    "slave_to_slave_coded": {
      "losses": 2,
      "desc": "Slave -> slave"
    }
  },
  "strategy": "master_to_slave_replica"
}
